{
  "name": "@alwaystrack/companion-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && vite build && node scripts/validate-build.mjs",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config vitest.config.ts",
    "coverage": "vitest run --config vitest.config.ts --coverage"
  },
  "dependencies": { "@alwaystrack/shared": "file:../../packages/shared" },
  "devDependencies": { "typescript": "^5.9.3", "vite": "^7.1.12", "vitest": "^3.2.6" }
}
